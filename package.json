{
  "name": "trading-journal-rules-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only harness for Firestore Security Rules tests. The app itself stays build-free: this package.json exists only to run the emulator test, never to build or bundle the app.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-trading-journal \"node test/rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.30.0"
  }
}
